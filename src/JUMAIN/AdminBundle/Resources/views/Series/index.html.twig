
<div id="mainObject" class="row mbl object-show">
  <div class="row mbl object-show">
    <div class="col-lg-12">
      <div class="panel">
          <a id="addObjBtn" to="/admin/seriz/add" class="btn btn-green"><i class="fa fa-upload"></i>&nbsp;
              Add TV-Show</a>
      </div>
    </div>
  </div>
  <div class="col-lg-12">
      <ul id="generalTab" class="nav nav-tabs responsive">
          <li class="active"><a href="#hollywood-tab" data-toggle="tab">Hollywood</a></li>
          <li><a href="#nollywood-tab" data-toggle="tab">Nollywood</a></li>
          <li><a href="#bollywood-tab" data-toggle="tab">Bollywood</a></li>
          <li><a href="#novelas-tab" data-toggle="tab">Novelas</a></li>
          <li><a href="#korean-tab" data-toggle="tab">Korean</a></li>
          <li><a href="#phillipines-tab" data-toggle="tab">Phillipines</a></li>
          <li><a href="#others-tab" data-toggle="tab">Other Regions</a></li>
      </ul>
      <div id="generalTabContent" class="tab-content responsive">
          <div id="hollywood-tab" class="tab-pane fade in active">
            <div class="row">
              <div class="col-md-12">
                <!--Series start-->
                <section class="panel">
                  <header class="panel-heading">
                    Hollywood Series
                  </header>
                  <div class="panel-body table-responsive">
                    {% if hollyEntities is empty %}
                        <p> No Series with the Hollywood category has been entered. Please Click on the Add button above to include new Series. </p>
                    {% else %}
                      <table class="table table-hover">
                        <thead>
                          <tr>
                            <th>#</th>
                            <th>Name of Series:</th>
                            <th>Description:</th>
                            <th>Year Produced</th>
                            <th>Number of Seasons</th>
                            <th></th>
                          </tr>
                        </thead>
                        <tbody>
                          {% for e in hollyEntities %}
                              <tr>
                                  <td>{{ e.id }}</td>
                                  <td>
                                      {% if e.serieName is empty %}
                                      {% else %}
                                      {{ e.serieName }}
                                      {% endif %}
                                  </td>
                                  <td>
                                      {% if e.description is empty %}
                                      {% else %}
                                      {{ e.description }}
                                      {% endif %}
                                  </td>
                                  <td>{{ e.dateProduced }}</td>
                                  <td>{{ e.seasonNumber }}</td>
                                  <td class="pull-right">
                                    <a class="btn btn-default btn-xs clearfix admin-open-seasons"  seaslnk="{{ path('jumain_admin_serie_seasons', {'serieId': e.id}) }}"><i class="fa fa-folder-open"></i></a>
                                    <a class="btn btn-default btn-xs"><i class="fa fa-pencil-square-o"></i></a>
                                    <a class="btn btn-default btn-xs"><i class="fa fa-trash"></i></a>
                                  </td>
                              </tr>
                          {% endfor %}
                        </tbody>
                      </table>
                    {% endif %}
                  </div>
                </section>
                <!--Series end-->
              </div><!--end col-12 -->
            </div>
          </div>
          <div id="nollywood-tab" class="tab-pane fade">
              <div class="row">
                <div class="col-md-12">
                  <!--Series start-->
                  <section class="panel">
                    <header class="panel-heading">
                      Nollywood Series
                    </header>
                    <div class="panel-body table-responsive">
                      {% if nollyEntities is empty %}
                          <p> No Series with the Nollywood category has been entered. Please Click on the Add button above to include new Series. </p>
                      {% else %}
                        <table class="table table-hover">
                          <thead>
                            <tr>
                              <th>#</th>
                              <th>Name of Series:</th>
                              <th>Description:</th>
                              <th>Year Produced</th>
                              <th>Number of Seasons</th>
                              <th></th>
                            </tr>
                          </thead>
                          <tbody>
                            {% for e in nollyEntities %}
                                <tr>
                                    <td>{{ e.id }}</td>
                                    <td>
                                        {% if e.serieName is empty %}
                                        {% else %}
                                        {{ e.serieName }}
                                        {% endif %}
                                    </td>
                                    <td>
                                        {% if e.description is empty %}
                                        {% else %}
                                        {{ e.description }}
                                        {% endif %}
                                    </td>
                                    <td>{{ e.dateProduced }}</td>
                                    <td>{{ e.seasonNumber }}</td>
                                    <td class="pull-right">
                                      <a class="btn btn-default btn-xs clearfix admin-open-seasons"  seaslnk="{{ path('jumain_admin_serie_seasons', {'serieId': e.id}) }}"><i class="fa fa-folder-open"></i></a>
                                      <a class="btn btn-default btn-xs"><i class="fa fa-pencil-square-o"></i></a>
                                      <a class="btn btn-default btn-xs"><i class="fa fa-trash"></i></a>
                                    </td>
                                </tr>
                            {% endfor %}
                          </tbody>
                        </table>
                      {% endif %}
                    </div>
                  </section>
                  <!--Series end-->
                </div><!--end col-12 -->
              </div>
          </div>
          <div id="bollywood-tab" class="tab-pane fade">
              <div class="row">
                <div class="col-md-12">
                  <!--Series start-->
                  <section class="panel">
                    <header class="panel-heading">
                      Bollywood Series
                    </header>
                    <div class="panel-body table-responsive">
                      {% if bollyEntities is empty %}
                          <p> No Series with the Bollywood category has been entered. Please Click on the Add button above to include new Series. </p>
                      {% else %}
                        <table class="table table-hover">
                          <thead>
                            <tr>
                              <th>#</th>
                              <th>Name of Series:</th>
                              <th>Description:</th>
                              <th>Year Produced</th>
                              <th>Number of Seasons</th>
                              <th></th>
                            </tr>
                          </thead>
                          <tbody>
                            {% for e in bollyEntities %}
                                <tr>
                                    <td>{{ e.id }}</td>
                                    <td>
                                        {% if e.serieName is empty %}
                                        {% else %}
                                        {{ e.serieName }}
                                        {% endif %}
                                    </td>
                                    <td>
                                        {% if e.description is empty %}
                                        {% else %}
                                        {{ e.description }}
                                        {% endif %}
                                    </td>
                                    <td>{{ e.dateProduced }}</td>
                                    <td>{{ e.seasonNumber }}</td>
                                    <td class="pull-right">
                                      <a class="btn btn-default btn-xs clearfix admin-open-seasons"  seaslnk="{{ path('jumain_admin_serie_seasons', {'serieId': e.id}) }}"><i class="fa fa-folder-open"></i></a>
                                      <a class="btn btn-default btn-xs"><i class="fa fa-pencil-square-o"></i></a>
                                      <a class="btn btn-default btn-xs"><i class="fa fa-trash"></i></a>
                                    </td>
                                </tr>
                            {% endfor %}
                          </tbody>
                        </table>
                      {% endif %}
                    </div>
                  </section>
                  <!--Series end-->
                </div><!--end col-12 -->
              </div>
          </div>
          <div id="novelas-tab" class="tab-pane fade">
              <div class="row">
                <div class="col-md-12">
                  <!--Series start-->
                  <section class="panel">
                    <header class="panel-heading">
                      Mexican/Spanish Series
                    </header>
                    <div class="panel-body table-responsive">
                      {% if novEntities is empty %}
                          <p> No Series with the Novelas category has been entered. Please Click on the Add button above to include new Series. </p>
                      {% else %}
                        <table class="table table-hover">
                          <thead>
                            <tr>
                              <th>#</th>
                              <th>Name of Series:</th>
                              <th>Description:</th>
                              <th>Year Produced</th>
                              <th>Number of Seasons</th>
                              <th></th>
                            </tr>
                          </thead>
                          <tbody>
                            {% for e in novEntities %}
                                <tr>
                                    <td>{{ e.id }}</td>
                                    <td>
                                        {% if e.serieName is empty %}
                                        {% else %}
                                        {{ e.serieName }}
                                        {% endif %}
                                    </td>
                                    <td>
                                        {% if e.description is empty %}
                                        {% else %}
                                        {{ e.description }}
                                        {% endif %}
                                    </td>
                                    <td>{{ e.dateProduced }}</td>
                                    <td>{{ e.seasonNumber }}</td>
                                    <td class="pull-right">
                                      <a class="btn btn-default btn-xs clearfix admin-open-seasons"  seaslnk="{{ path('jumain_admin_serie_seasons', {'serieId': e.id}) }}"><i class="fa fa-folder-open"></i></a>
                                      <a class="btn btn-default btn-xs"><i class="fa fa-pencil-square-o"></i></a>
                                      <a class="btn btn-default btn-xs"><i class="fa fa-trash"></i></a>
                                    </td>
                                </tr>
                            {% endfor %}
                          </tbody>
                        </table>
                      {% endif %}
                    </div>
                  </section>
                  <!--Series end-->
                </div><!--end col-12 -->
              </div>
          </div>
          <div id="korean-tab" class="tab-pane fade">
              <div class="row">
                <div class="col-md-12">
                  <!--Series start-->
                  <section class="panel">
                    <header class="panel-heading">
                      Korean Series
                    </header>
                    <div class="panel-body table-responsive">
                      {% if korEntities is empty %}
                          <p> No Serie with the Korean category has been entered. Please Click on the Add button above to include new Series. </p>
                      {% else %}
                        <table class="table table-hover">
                          <thead>
                            <tr>
                              <th>#</th>
                              <th>Name of Series:</th>
                              <th>Description:</th>
                              <th>Year Produced</th>
                              <th>Number of Seasons</th>
                              <th></th>
                            </tr>
                          </thead>
                          <tbody>
                            {% for e in korEntities %}
                                <tr>
                                    <td>{{ e.id }}</td>
                                    <td>
                                        {% if e.serieName is empty %}
                                        {% else %}
                                        {{ e.serieName }}
                                        {% endif %}
                                    </td>
                                    <td>
                                        {% if e.description is empty %}
                                        {% else %}
                                        {{ e.description }}
                                        {% endif %}
                                    </td>
                                    <td>{{ e.dateProduced }}</td>
                                    <td>{{ e.seasonNumber }}</td>
                                    <td class="pull-right">
                                      <a class="btn btn-default btn-xs clearfix admin-open-seasons"  seaslnk="{{ path('jumain_admin_serie_seasons', {'serieId': e.id}) }}"><i class="fa fa-folder-open"></i></a>
                                      <a class="btn btn-default btn-xs"><i class="fa fa-pencil-square-o"></i></a>
                                      <a class="btn btn-default btn-xs"><i class="fa fa-trash"></i></a>
                                    </td>
                                </tr>
                            {% endfor %}
                          </tbody>
                        </table>
                      {% endif %}
                    </div>
                  </section>
                  <!--New Series end-->
                </div><!--end col-12 -->
              </div>
          </div>
          <div id="phillipines-tab" class="tab-pane fade">
              <div class="row">
                <div class="col-md-12">
                  <!--Series start-->
                  <section class="panel">
                    <header class="panel-heading">
                      Phillipines Series
                    </header>
                    <div class="panel-body table-responsive">
                      {% if philEntities is empty %}
                          <p> No Serie with the Phillipines category has been entered. Please Click on the Add button above to include new Series. </p>
                      {% else %}
                        <table class="table table-hover">
                          <thead>
                            <tr>
                              <th>#</th>
                              <th>Name of Series:</th>
                              <th>Description:</th>
                              <th>Year Produced</th>
                              <th>Number of Seasons</th>
                              <th></th>
                            </tr>
                          </thead>
                          <tbody>
                            {% for e in philEntities %}
                                <tr>
                                    <td>{{ e.id }}</td>
                                    <td>
                                        {% if e.serieName is empty %}
                                        {% else %}
                                        {{ e.serieName }}
                                        {% endif %}
                                    </td>
                                    <td>
                                        {% if e.description is empty %}
                                        {% else %}
                                        {{ e.description }}
                                        {% endif %}
                                    </td>
                                    <td>{{ e.dateProduced }}</td>
                                    <td>{{ e.seasonNumber }}</td>
                                    <td class="pull-right">
                                      <a class="btn btn-default btn-xs clearfix admin-open-seasons"  seaslnk="{{ path('jumain_admin_serie_seasons', {'serieId': e.id}) }}"><i class="fa fa-folder-open"></i></a>
                                      <a class="btn btn-default btn-xs"><i class="fa fa-pencil-square-o"></i></a>
                                      <a class="btn btn-default btn-xs"><i class="fa fa-trash"></i></a>
                                    </td>
                                </tr>
                            {% endfor %}
                          </tbody>
                        </table>
                      {% endif %}
                    </div>
                  </section>
                  <!--Series end-->
                </div><!--end col-12 -->
              </div>
          </div>
          <div id="others-tab" class="tab-pane fade">
              <div class="row">
                <div class="col-md-12">
                  <!--Series start-->
                  <section class="panel">
                    <header class="panel-heading">
                      Other Series
                    </header>
                    <div class="panel-body table-responsive">
                      {% if otherEntities is empty %}
                          <p> No Series with the Others category has been entered. Please Click on the Add button above to include new Series. </p>
                      {% else %}
                        <table class="table table-hover">
                          <thead>
                            <tr>
                              <th>#</th>
                              <th>Name of Series:</th>
                              <th>Description:</th>
                              <th>Year Produced</th>
                              <th>Number of Seasons</th>
                              <th></th>
                            </tr>
                          </thead>
                          <tbody>
                            {% for e in otherEntities %}
                                <tr>
                                    <td>{{ e.id }}</td>
                                    <td>
                                        {% if e.serieName is empty %}
                                        {% else %}
                                        {{ e.serieName }}
                                        {% endif %}
                                    </td>
                                    <td>
                                        {% if e.description is empty %}
                                        {% else %}
                                        {{ e.description }}
                                        {% endif %}
                                    </td>
                                    <td>{{ e.dateProduced }}</td>
                                    <td>{{ e.seasonNumber }}</td>
                                    <td class="pull-right">
                                      <a class="btn btn-default btn-xs clearfix admin-open-seasons"  seaslnk="{{ path('jumain_admin_serie_seasons', {'serieId': e.id}) }}"><i class="fa fa-folder-open"></i></a>
                                      <a class="btn btn-default btn-xs"><i class="fa fa-pencil-square-o"></i></a>
                                      <a class="btn btn-default btn-xs"><i class="fa fa-trash"></i></a>
                                    </td>
                                </tr>
                            {% endfor %}
                          </tbody>
                        </table>
                      {% endif %}
                    </div>
                  </section>
                  <!--Series end-->
                </div><!--end col-12 -->
              </div>
          </div>
      </div>
  </div>
</div>
<div id="addObject" class="row mbl center object-hide">
    <div class="col-lg-4 col-lg-offset-4">
        <div class="panel panel-grey">
            <div class="panel-heading">
                Add Series</div>
            <div class="panel-body pan">
                {{ form_start(form) }}
                    {{ form_errors(form) }}
                <div class="form-body pal">
                    <div class="form-group">
                        <div class="input-icon right">
                            {{ form_errors(form.serieName) }}
                            <i class="fa fa-user"></i>
                            {{ form_widget(form.serieName, {'attr': {'class': 'form-control', 'placeholder':'Name of Series', 'autofocus':''}}) }}
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="input-icon right">
                            {{ form_errors(form.category) }}
                            {{ form_widget(form.category, {'attr': {'class': 'form-control', 'placeholder':'Category', 'autofocus':''}}) }}
                        </div>
                    </div>
                    <div class="form-group">
                        {{ form_errors(form.description) }}
                        {{ form_widget(form.description, {'attr': {'class': 'form-control','rows':'5', 'placeholder':'Description', 'autofocus':''}}) }}
                    </div>
                    <div class="form-group">
                        <div class="input-icon right">
                            {{ form_errors(form.dateProduced) }}
                            <i class="fa fa-calendar"></i>
                            {{ form_widget(form.dateProduced, {'attr': {'class': 'form-control', 'placeholder':'Year Produced', 'autofocus':''}}) }}
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="input-icon right">
                            {{ form_label(form.imageFile) }}
                            {{ form_errors(form.imageFile) }}
                            <i class="fa fa-picture-o"></i>
                            {{ form_widget(form.imageFile, {'attr': {'class': '', 'placeholder':'Choose Picture File', 'autofocus':''}}) }}
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="input-icon right">
                            {{ form_label(form.bannerImageFile) }}
                            {{ form_errors(form.bannerImageFile) }}
                            <i class="fa fa-picture-o"></i>
                            {{ form_widget(form.bannerImageFile, {'attr': {'class': '', 'placeholder':'Choose Banner File', 'autofocus':''}}) }}
                        </div>
                    </div>
                </div>
                <div class="form-actions text-right pal">
                  <a href="#" class="btn cancel-add">Cancel</a>&nbsp;&nbsp;
                    {{ form_widget(form.submit, {'attr': {'class': 'btn btn-primary'}}) }}
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
</div>
<section id="seasonSection" class="panel row mbl object-hide">
  <header class="panel-heading">
    Seasons
  </header>
  <div class="panel-body table-responsive">
      <div id="seasonCont" class="col-lg-12 panel-body table-responsive">
          Seasons
          <a class="btn btn-default btn-xs admin-open-episodes" epilnk="testin_epi_url"><i class="fa fa-folder-open"></i></a>
      </div>
  </div>
  <footer class="panel-footer pull-right">
    <a id="cancel-seasons" class="btn btn-primary">Cancel</a>
  </footer>
</section>
<section id="episodeSection" class="panel row mbl object-hide">
  <header class="panel-heading">
    Episodes
  </header>
  <div class="panel-body table-responsive">
      <div id="episodeCont" class="col-lg-12 panel-body table-responsive">
          Episodes
      </div>
  </div>
  <footer class="panel-footer pull-right">
    <a id="cancel-episodes" class="btn btn-primary">Cancel</a>
  </footer>
</section>
<script>
//For my re-arranging blocks on add object
$('a#addObjBtn').bind('click',function(e){
  e.preventDefault();

  console.log("To check if working");
  $('#mainObject').removeClass('object-show');
  $('#mainObject').addClass('object-hide');
  $('#addObject').removeClass('object-hide');
  $('#addObject').addClass('object-show');
});

$('a.cancel-add').bind('click',function(e){
  e.preventDefault();

  console.log("To check cancel if working");

  $('#addObject').removeClass('object-show');
  $('#addObject').addClass('object-hide');

  $('#mainObject').removeClass('object-hide');
  $('#mainObject').addClass('object-show');
});
{#
$( '#formSerie' ).submit(function(e) {
  //alert( "Handler for .submit() called." );
  e.preventDefault();

  $this = $(this);
  $url = $this.attr("action");
  $('#content').hide().parent().append('<div id="loading" class="center"><i class="fa fa-cog fa-spin fa-3x stat-elem lg loading-icon"></i>Saving...<div class="center"></div></div>');

  $.ajax({
      // the URL for the request
      url: $url,
      // whether this is a POST or GET request
      type: "POST",
      // the type of data we expect back
      data: $this.serialize(),
      // code to run if the request succeeds;
      // the response is passed to the function
      success: function () {
          //$('#content').html(html);
          //$( "<h1/>" ).text( json.title ).appendTo( "body" );
          //$( "<div class=\"content\"/>").html( json.html ).appendTo( "body" );
      },
      // code to run if the request fails; the raw request and
      // status codes are passed to the function
      error: function (xhr, status, errorThrown) {
          alert("Sorry, there was a problem!");
          //console.log( "Error: " + errorThrown );
          //console.log( "Status: " + status );
          //console.dir( xhr );
      },
      // code to run regardless of success or failure
      complete: function (xhr, status) {
          alert( "Submitted Succesfully" );
          $('#loading').remove();
          $('#content').fadeIn();
      }
  });
});
#}
// Opening seasons in dash
$('a.admin-open-seasons').bind('click',function(e){
  e.preventDefault();

  console.log("To check if working");
  $('#mainObject').removeClass('object-show');
  $('#mainObject').addClass('object-hide');
  $('#seasonSection').removeClass('object-hide');
  $('#seasonSection').addClass('object-show');

  $this = $(this);
  $url = $this.attr("seaslnk");

  console.log($url);

  $('#seasonCont').hide().parent().append('<div id="loading" class="center"><i class="fa fa-refresh fa-spin fa-3x stat-elem lg loading-icon"></i>Loading...<div class="center"></div></div>');
  $.ajax({
      // the URL for the request
      url: $url,
      // whether this is a POST or GET request
      type: "GET",
      // the type of data we expect back
      data: $this.serialize(),
      // code to run if the request succeeds;
      // the response is passed to the function
      success: function (html) {
          $('#seasonCont').html(html);
          //$( "<h1/>" ).text( json.title ).appendTo( "body" );
          //$( "<div class=\"content\"/>").html( json.html ).appendTo( "body" );
      },
      // code to run if the request fails; the raw request and
      // status codes are passed to the function
      error: function (xhr, status, errorThrown) {
          alert("Sorry, there was a problem!");
          $('#seasonCont').append('<div>Error returned.</div>');
          console.log( "Error: " + errorThrown );
          console.log( "Status: " + status );
          console.dir( xhr );
      },
      // code to run regardless of success or failure
      complete: function (xhr, status) {
          //alert( "Submitted Succesfully" );
          $('#loading').remove();
          $('#seasonCont').fadeIn();
      }
  });

});

$('a#cancel-seasons').bind('click',function(e){
  e.preventDefault();

  console.log("To check cancel if working");

  $('#seasonSection').removeClass('object-show');
  $('#seasonSection').addClass('object-hide');
  $('#mainObject').removeClass('object-hide');
  $('#mainObject').addClass('object-show');
});
</script>
